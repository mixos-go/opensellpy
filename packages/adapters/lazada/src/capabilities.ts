import { DOMAIN_KEYS, type DomainKey } from '@mixos-go/opensellpy-core'

/** Lazada mendukung kelima domain contract opensellpy. */
export const LAZADA_CAPABILITIES: readonly DomainKey[] = DOMAIN_KEYS